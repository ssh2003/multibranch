clone:
  default:
    image: plugins/git
    depth: 50
    recursive: false
    
pipeline:
  resolve:
    image: alpine
    commands:
      - cat /etc/resolv.conf
      - echo "BBBBBB"
      - ping -c 3 ukr.net
    when:
      status: [ success, failure ]
